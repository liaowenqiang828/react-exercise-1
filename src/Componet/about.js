import React, {Component} from 'react';
import '../styles/about.css';

class About extends Component {
    render() {
        return <div className="redHead">
            <h3>ABOUT ME</h3>
            <p>That is the basic structure of an image tag. With React, the same structure applies, there are just two changes between how you would add images with html and how you add pictures with React.</p>
        </div>
    }
}

export default About;
